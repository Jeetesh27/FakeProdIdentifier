{
  "name": "fake-product-identification",
  "version": "1.0.0",
  "description": "project on blockchain",
  "main": "index.js",
  "scripts": {
    "copy-contracts-build": "echo \"Copying build/contracts/* files into client/src/contracts\" && mkdir -p client/src/contracts && cp -r build/contracts/* client/src/contracts",
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "html5-qrcode": "^2.2.0",
    "lite-server": "2.6"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.0.15",
    "cors": "^2.8.5"
  }
}
